# Database Configuration
# Lấy từ Railway.app MySQL service
DB_HOST=containers-us-west-xxx.railway.app
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=railway
DB_PORT=3306

# Server Port (Railway tự động set PORT, không cần set nếu deploy)
PORT=3000
